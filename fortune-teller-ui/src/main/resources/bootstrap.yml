spring:
  application:
    name: ui
  cloud:
    services:
      registrationMethod: route
    config:
      uri: http://localhost:8888

producer:
  ribbon:
    ServerListRefreshInterval: 5000
ribbon:
  IsSecure: false
